from "lib:cif3" import *;

cif3merge(                      // 3 files to merge
    "20_control.cif",
    "plant_for_control.cif",
    "svg.cif",
//     "-o merged.cif"             // -o option for output file, default is merged.cif
);

cif3sim(
    "merged.cif",
    "--option-dialog=no",
//     "-t 4",
    "--distributions-seed=3",

    "-i svg",
    "-a first",

//     "--stateviz=1",
//     "--env-events=svg",

    "--frame-rate=50",
    "--speed=1",

    "--solver-root-maxchk=0.1",
);
